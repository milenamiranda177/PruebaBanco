git:
  depth: 3
language: java
jdk:
  - oraclejdk11
services:
  - postgresql
before_script:
  - psql -c 'create database spring;' -U postgres
  - psql -e 'USE spring; create table factura(numero int primary key, concepto character varying(5), importe numeric);' -U postgres
#before_install:
#  - chmod +x ./mvnw
